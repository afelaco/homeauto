{
  "config": {
    "abort": {
      "already_configured": "Only a single instance of a device is allowed.",
      "single_instance_allowed": "Only a single instance is allowed."
    },
    "error": {
      "auth": "Username, Password, or Address is wrong."
    },
    "step": {
      "name": {
        "data": {
          "name": "Device Name"
        },
        "title": "Configure Device Name"
      },
      "user": {
        "data": {
          "address": "Address",
          "channel": "Channel (single cameras are 0, NVRs are the 0 based index)",
          "events": "Events",
          "password": "Password",
          "port": "Port",
          "rtsp_port": "RTSP Port",
          "streams": "RTSP Streams",
          "username": "Username"
        },
        "description": "Example address: 192.168.1.108",
        "title": "Add Dahua Camera"
      }
    }
  },
  "options": {
    "step": {
      "user": {
        "data": {
          "binary_sensor": "Binary sensor enabled",
          "camera": "Camera enabled",
          "light": "Light enabled",
          "select": "Select enabled",
          "sensor": "Sensor enabled",
          "switch": "Switch enabled"
        }
      }
    }
  }
}
